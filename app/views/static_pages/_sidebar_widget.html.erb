<div class="flickr">
  <ul>
    <% photos.each do |row| -%>
      <li>
        <% row.each do |p| %>
          <%= link_to(image_tag("#{p}", :class => 'flickr_photo', :border => 0, :size => '75x75'), "#{p}", target: "_blank") %>
        <% end %>
      </li>
    <% end -%>
  </ul>
</div>
